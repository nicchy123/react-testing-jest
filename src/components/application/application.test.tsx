import { render } from "@testing-library/react";
import { Application } from "./appliation";

describe("Application", () => {
  test("renders correctly", () => {
    render(<Application />); 
  });
});